#!/bin/bash

# Extract and format the Pokémon name (capitalize first letter)
name=$(jq -r '.name' data.json | sed 's/.*/\u&/')

# Extract height (in decimeters) and convert to meters
height=$(jq -r '.height' data.json | awk '{print $1 / 10}')

# Extract weight (in hectograms) and convert to kilograms
weight=$(jq -r '.weight' data.json | awk '{print $1 / 10}')

# Extract first type and capitalize
type=$(jq -r '.types[0].type.name' data.json | sed 's/.*/\u&/')

# Print formatted output
echo "$name is of type $type, weighs ${weight}kg, and is ${height}m tall."
